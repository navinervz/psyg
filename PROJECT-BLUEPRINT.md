# نقشهٔ ساخت پروژه — بر پایهٔ سای‌جی

> این سند برای شروع یک پروژهٔ تازه در حوزه‌ای دیگر (پوشاک، زغال، لوازم خانگی، هر چیز) با همان معماری سای‌جی نوشته شده.
>
> **طرز استفاده:** یک چت تازه باز کن، این فایل را بده و بگو «این را برای حوزهٔ فلان بساز». بعد لوگو، پالت رنگی و فونت‌ها را بده.

---

## بخش صفر — قبل از هر چیز، این را بخوان

سای‌جی یک سایت افیلیت نیست که اتفاقی درست شده باشد. یک **تصمیم مرکزی** دارد و هر خط کدش از همان می‌آید:

> سایت فقط چیزی را ادعا می‌کند که خودش اندازه گرفته باشد.

این جمله ساده به‌نظر می‌رسد ولی بیشتر باگ‌های این پروژه از نقض همین بود. نمونه‌های واقعی:

| چه شد | چرا خطرناک بود |
|---|---|
| فروشگاه «۹۷٪ تخفیف» اعلام کرد؛ سایت نشانش داد | تخفیف واقعی نبود، قیمت قبلی جعلی بود |
| محصول تازه‌وارد «این هفته ارزان شد» گرفت | فقط یک قیمت از آن دیده بودیم |
| کاتالوگ خوانده نشد؛ سایت ۵۱ محصول نمونه سرو کرد | کاربر روی محصولی کلیک کرد که وجود نداشت |
| ایمیل کاربران فقط در لاگ نوشته می‌شد | «ثبت شد» می‌دیدند و هیچ‌جا ذخیره نمی‌شد |

**هر چهارتا یک الگو دارند: کد کار می‌کرد، خطایی نمی‌داد، و چیز اشتباه نشان می‌داد.** این خطرناک‌ترین نوع باگ است، و بیشتر تست‌های این پروژه دقیقاً برای گرفتن همین نوشته شده‌اند.

اگر پروژهٔ تازه‌ات این اصل را نداشته باشد، بقیهٔ این سند فقط یک قالب خالی است.

---

## بخش یک — نقشهٔ کلی

```
┌─────────────────┐     ┌──────────────┐     ┌─────────────────┐
│  منبع داده      │ ──▶ │  n8n         │ ──▶ │  سایت Next.js   │
│  (API فروشنده)  │     │  ورک‌فلوها   │     │  + والیوم داده  │
└─────────────────┘     └──────────────┘     └─────────────────┘
                              │                      │
                              │                      ▼
                              │              ┌─────────────────┐
                              └────────────▶ │ تلگرام / ایمیل  │
                                             └─────────────────┘
```

**سه لایه، سه مسئولیت جدا:**

1. **n8n** داده می‌گیرد، تمیز می‌کند، تاریخچه می‌سازد، و به سایت می‌فرستد. محتوا هم تولید می‌کند.
2. **سایت** فقط می‌خواند و نمایش می‌دهد. هیچ‌وقت خودش از منبع بیرونی نمی‌خواند.
3. **والیوم داکر** حافظهٔ بلندمدت است. بین دیپلوی‌ها می‌ماند.

چرا این جدایی مهم است: اگر سایت مستقیم از API فروشنده می‌خواند، هر کندی یا قطعی آن‌ها به کندی سایت تبدیل می‌شد، و تاریخچهٔ قیمت هیچ‌وقت ساخته نمی‌شد.

---

## بخش دو — ساختار فایل‌ها

### `src/lib/` — مغز پروژه

| فایل | کارش | برای پروژهٔ تازه |
|---|---|---|
| `types.ts` | تعریف `Product`، `Category`، `Store`، `PricePoint` | **عوض می‌شود** — مدل دامنهٔ تو |
| `site.ts` | نام برند، دامنه، ایمیل، تیتر هیرو | **عوض می‌شود** |
| `reference.ts` | دادهٔ سبک: دسته‌ها، فروشگاه‌ها، ویژگی‌ها | **عوض می‌شود** |
| `data.ts` | کاتالوگ سنگین + سلکتورها | ساختار می‌ماند |
| `format.ts` | قالب‌بندی عدد/قیمت/درصد + `priceStanding` | تقریباً می‌ماند |
| `catalog-store.ts` | ادغام کاتالوگ تازه با تاریخچهٔ قبلی | می‌ماند |
| `product-key.ts` | شناسایی محصول مستقل از فروشگاه | منطقش می‌ماند، الگوها عوض می‌شود |
| `admin-store.ts` | لایهٔ بازنویسی ادمین | می‌ماند |
| `article-store.ts` | ذخیرهٔ مقاله‌های تولیدشده | می‌ماند |
| `subscriber-store.ts` | ایمیل مشترکان | می‌ماند |
| `schemas.ts` | اعتبارسنجی ورودی با zod | گسترش می‌یابد |
| `affilio.ts` | آداپتور منبع داده | **کاملاً عوض می‌شود** |
| `mcp/` | اندپوینت MCP برای مدل‌های هوش مصنوعی | می‌ماند |
| `security-audit.ts` | ممیزی خودکار پیکربندی | می‌ماند |

### `src/app/api/` — اندپوینت‌ها

| مسیر | متد | احراز هویت | کار |
|---|---|---|---|
| `/api/ingest` | POST | توکن | n8n می‌گوید «برو داده بگیر» — **بدنه نمی‌پذیرد** |
| `/api/content` | POST/GET/DELETE | توکن جدا | مقاله‌های تولیدشده |
| `/api/feed` | GET | ندارد | کاتالوگ عمومی برای ربات تلگرام |
| `/api/deals` | GET | ندارد | فرصت‌ها برای خبرنامه |
| `/api/search` | GET | ندارد | جستجوی زنده |
| `/api/assistant` | POST | ندارد | مشاور خرید |
| `/api/subscribe` | POST | ندارد | ثبت ایمیل |
| `/api/subscribers` | GET | توکن جدا | فهرست مشترکان |
| `/api/contact` | POST | ندارد | فرم تماس |
| `/api/admin/*` | POST | رمز | پنل مدیریت |
| `/api/mcp` | POST | توکن جدا | ابزار برای مدل‌های AI |
| `/api/health` | GET | ندارد | وضعیت سلامت |

**قاعدهٔ توکن‌ها: هر کدام جداست.** لو رفتن یکی، بقیه را باز نمی‌کند.

### `src/components/` — دسته‌بندی

```
layout/      Header، Footer، PageShell، MobileTabBar، MainNav
ui/          Card، Button، Input، Badge، Chip، EmptyState، Brand
deals/       DealCard، DealGrid، PriceBlock، PriceSparkline، BuyButton
product/     PriceHistoryChart، BuyVerdictCard، HistorySourceNote
hero/        HeroSection، AiSearchBar، RobotMascot، CategoryChips
mobile/      MobileHero، MobileCategoryRail، MobileTopDrop
sidebar/     NewArrivalsCard، RecentAlertsCard، NotifyMeWidget
chat/        ChatPanel، useAssistantChat
marketing/   FeatureStrip، PartnerMarquee
mag/         ArticleBody، ArticleCta
account/     AccountTabs، SavedProductsGrid، PurchaseHistory
admin/       AdminLogin، AdminDashboard
contact/     ContactForm
```

---

## بخش سه — تصمیم‌های معماری و دلیلشان

هر کدام از این‌ها از یک اشتباه واقعی آمده. **بدون دلیل کپی نکن، ولی دلیل را بخوان.**

### ۱. کاتالوگ در والیوم داکر، نه در ریپو

فایل `catalog.json` روی والیوم است و n8n می‌نویسدش. سایت فقط می‌خواند.

**چرا:** داده هر روز عوض می‌شود، کد هفته‌ای یک بار. اگر داده در ریپو بود، هر به‌روزرسانی قیمت یک کامیت می‌شد.

**نتیجهٔ ناخواسته‌ای که باید بدانی:** والیوم موقع بیلد مانت نیست. پس صفحه‌هایی که کاتالوگ زنده می‌خوانند باید `force-dynamic` باشند، وگرنه Next در زمان بیلد پیش‌رندرشان می‌کند و دادهٔ نمونه در HTML حک می‌شود. تست `static-data.test.ts` این را خودکار می‌سنجد.

### ۲. تاریخچهٔ قیمت هیچ‌وقت جعل نمی‌شود

محصول تازه با **یک نقطه** شروع می‌شود، نه سی نقطهٔ ساختگی.

**چرا:** نمودار جعلی دقیقاً همان چیزی است که کل ارزش سایت را از بین می‌برد. هزینه‌اش این است که هفتهٔ اول نمودارها خالی‌اند — و آن هزینه را می‌پذیریم.

### ۳. لایهٔ ادمین در فایل جدا

`admin.json` جدا از `catalog.json` است.

**چرا:** همگام‌سازی هر بار کل کاتالوگ را بازنویسی می‌کند. اگر تغییرات دستی آنجا بودند، اولین اجرای بعدی پاکشان می‌کرد.

### ۴. «پنهان کردن» نه «حذف»

محصولی که ادمین حذف می‌کند، اجرای بعدی دوباره از منبع می‌آید. پس شناسه‌اش نگه داشته می‌شود و هر بار کنار گذاشته می‌شود.

### ۵. روی سرور، دادهٔ نمونه ممنوع

پرچم `PSYG_REQUIRE_CATALOG=1` یعنی اگر کاتالوگ خوانده نشد، صفحه خالی بماند.

**چرا:** یک بار سایت ۵۱ محصول جعلی سرو کرد و هیچ خطایی جایی ثبت نشد. **خالی از جعلی بهتر است** — چون خالی را کسی می‌بیند و جعلی را نه.

### ۶. لینک افیلیت ذخیره می‌شود، ساخته نمی‌شود

فرض اولیه این بود که می‌شود از روی آدرس محصول لینک افیلیت ساخت. دو لینک واقعی مقایسه شد: بعضی پارامترها ثابت‌اند ولی یکی برای هر لینک یکتاست.

**درس عمومی:** قبل از ساختن منطق روی یک فرض دربارهٔ داده، **دو نمونهٔ واقعی را با هم مقایسه کن.**

### ۷. عدد را ما می‌نویسیم، مدل نه

در همهٔ جاهایی که مدل زبانی متن می‌سازد (مقاله، پست تلگرام، مشاور خرید)، قیمت و درصد را کد از دادهٔ واقعی اضافه می‌کند.

**چرا:** مدل عدد را اشتباه بازنویسی می‌کند، و قیمتی که با صفحهٔ محصول نخواند همان لحظه لو می‌رود.

### ۸. هر خروجی مدل، نگهبان دارد

مدل‌ها گاهی JSON خام، گاهی داخل ```` ``` ````، گاهی JSON داخل یک فیلد رشته‌ای برمی‌گردانند. کد باید هر سه را باز کند و اگر باز نشد، **متن خام را نشان ندهد**.

### ۹. سه‌حالته، نه دوحالته

روند قیمت سه حالت دارد: `drop`، `rise`، `unknown`.

**چرا:** با منطق دوحالتی (`delta < 0`)، محصولی که فقط یک قیمت دارد در دستهٔ «گران شده» می‌افتاد و قرمز می‌شد. **وقتی نمی‌دانیم، ادعا نمی‌کنیم.**

### ۱۰. منطق مشترک یک جا

`priceTrend` و `priceStanding` در `format.ts` هستند و همه از همان‌جا می‌خوانند.

**چرا:** یک بار این منطق در چند کامپوننت تکرار شده بود؛ یکی درست شد و بقیه نه. نتیجه: بج می‌گفت «تازه» ولی قیمت کنارش قرمز بود.

---

## بخش چهار — ورک‌فلوهای n8n

| شماره | نام | کار | زمان‌بندی |
|---|---|---|---|
| ۰۰ | AI Gateway | چرخش بین کلیدهای مدل | زیرورک‌فلو |
| ۲۰ | Catalog Sync | گرفتن داده و فرستادن به `/api/ingest` | چند ساعت یک بار |
| ۲۱ | Shopping Assistant | مشاور خرید سایت | وبهوک |
| ۲۲ | Content Generator | تولید مقاله + بازبینی و حذف مقالهٔ خراب | هر دو روز |
| ۲۳ | Newsletter | خبرنامهٔ هفتگی | دوشنبه صبح |
| ۲۴ | Telegram Channel | پست خودکار در کانال | هر ۲ ساعت |

### درس‌های n8n که وقت زیادی گرفتند

**۱. شکل پاسخ خطا با پاسخ موفق فرق دارد.**
با `fullResponse`، بدنه در `body` می‌آید — ولی وقتی درخواست خطا می‌دهد و `neverError` روشن است، همان بدنه در `data` می‌آید. یک بار از روی یک پاسخ ۴۰۴ نتیجه گرفتم کلید `data` است؛ بعد که اندپوینت درست شد، همان کد خراب شد. **هر دو را بپذیر.**

**۲. حافظهٔ ورک‌فلو در اجرای دستی ذخیره نمی‌شود.**
`$getWorkflowStaticData` فقط در اجرای زمان‌بندی‌شده ماندگار است. دکمهٔ Execute حافظه را ذخیره نمی‌کند — پس دو اجرای دستی پشت سر هم، یک پست را دو بار می‌فرستند.

**۳. تریگر زمان‌بندی از بیرون قابل صدا زدن نیست.** فقط webhook و form و chat.

**۴. ثبت در حافظه قبل از ارسال، نه بعدش.** اگر تلگرام خطا داد، دور بعد سراغ موضوع دیگری می‌رود. **تکرار نشدن مهم‌تر از پست شدن یک مورد است.**

### الگوی «چیزی برای گفتن هست؟»

هر ورک‌فلوی تولید محتوا یک گیت دارد که اگر موضوعی نبود، ساکت می‌ماند و **دلیلش را در خروجی می‌نویسد** (`NOTHING_NEW`، `TOO_SOON`، `BAD_FEED`).

**چرا:** بدون دلیل، «چیزی نبود» و «چیزی خراب است» یک شکل دیده می‌شوند.

---

## بخش پنج — تست‌ها

۳۶ فایل تست یونیت، بیش از ۴۰۰ تست. ولی **تعداد مهم نیست، نوعشان مهم است.**

### چهار دسته

**۱. تست‌های صداقت** — سایت چیزی ادعا نکند که نمی‌داند
```
honesty.test.ts        هیچ ادعای اغراق‌آمیزی در متن‌ها نیست
honest-discount.test.ts تخفیف غیرواقعی باور نمی‌شود
standing.test.ts       فقط وقتی افت گزارش شود که واقعاً باشد
static-data.test.ts    دادهٔ زنده در زمان بیلد حک نمی‌شود
disclosure.test.ts     افشای کمیسیون در همهٔ صفحه‌ها هست
```

**۲. تست‌های یکپارچگی** — چیزی نصفه نماند
```
routes.test.ts    هیچ لینک مرده‌ای نیست، هر محصول صفحه دارد
data.test.ts      شناسه‌ها یکتا، قیمت‌ها مثبت، دسته‌ها معتبر
production.test.ts هر اندپوینت پرهزینه سقف نرخ دارد
```

**۳. تست‌های رابط کاربری** — چیزی که فقط روی دستگاه واقعی دیده می‌شود
```
design.test.ts     کنتراست متن، حلقهٔ فوکوس، کاهش حرکت
mobile.test.ts     زوم سافاری، اندازهٔ هدف لمسی، سرریز
neon-edge.test.ts  ماسک وبکیت، گارد hover، @property
```

**۴. تست‌هایی که خودِ تست را می‌سنجند**

این دستهٔ چهارم را جدی بگیر. چند بار تستی نوشتم که **روی کامنت خودش** قرمز یا سبز شد. الگویش این است:

```ts
test("خودِ تست سالم است", () => {
  assert.ok(detect(BAD_SAMPLE),  "الگوی خراب را واقعاً پیدا می‌کند");
  assert.ok(!detect(GOOD_SAMPLE), "نمونهٔ سالم را اشتباه علامت نمی‌زند");
});
```

**قاعده: هر تستی که سورس را با رجکس می‌خواند، اول کامنت‌ها را حذف کند.**

```ts
const code = source
  .replace(/\{\/\*[\s\S]*?\*\/\}/g, "")
  .replace(/\/\*[\s\S]*?\*\//g, "")
  .replace(/\/\/.*$/gm, "");
```

### تستی که ارزشش از کد بیشتر است

```ts
test("متغیرها واقعاً به کانتینر می‌رسند", () => {
  const compose = readFileSync("docker-compose.yml", "utf8");
  assert.match(compose, /RESEND_API_KEY:\s*\$\{RESEND_API_KEY/);
});
```

گذاشتن متغیر در `.env` کافی نیست؛ اگر در `docker-compose.yml` اعلام نشود به کانتینر نمی‌رسد. **هیچ خطایی هم نمی‌دهد.**

---

## بخش شش — دیزاین

### توکن‌های رنگ (این‌ها را عوض می‌کنی)

```css
:root {
  --color-night:    #08090a;   /* پس‌زمینهٔ اصلی */
  --color-surface:  #0f1110;   /* کارت */
  --color-elevated: #1c211d;   /* سطح بالاتر */
  --color-line:     #2b322c;   /* حاشیه */

  --color-accent:      #a3e635; /* رنگ برند */
  --color-accent-dim:  #7cb518;
  --color-accent-deep: #4d7c0f;

  --color-danger:     #ff4d4d;
  --color-hi:  #f2f5f0;   /* متن اصلی */
  --color-mid: #a8b0a4;   /* متن فرعی */
  --color-low: #828a7e;   /* متن کم‌رنگ */

  --radius-card:  20px;
  --radius-inner: 14px;
  --radius-pill:  999px;
}
```

**⚠️ هر رنگ متن را با کنتراست بسنج، نه با چشم.** رنگ `low` قبلاً `#6b7268` بود و روی هر سه سطح زیر ۴.۵ می‌افتاد — دقیقاً همان رنگی که برای متن‌های ۱۰ پیکسلی استفاده می‌شود. تست `design.test.ts` این را خودکار می‌سنجد؛ **آن تست را نگه دار.**

### فونت

```css
@font-face {
  font-family: "YourFont";
  src: url("/fonts/YourFont.woff2") format("woff2");
  font-weight: 400;          /* بازهٔ 100 900 اعلام نکن */
  font-display: swap;
}
```

**دو نکته که وقت گرفتند:**

۱. اگر بازهٔ وزن `100 900` اعلام کنی، مرورگر فکر می‌کند همهٔ وزن‌ها پوشش داده شده و کل متن یکدست نازک می‌شود.

۲. **فونت را preload کن.** بدون آن دانلود در میلی‌ثانیهٔ ۱۲۰۰ شروع می‌شود (بعد از HTML → CSS → کشف فونت). با preload از لحظهٔ اول:

```html
<link rel="preload" href="/fonts/YourFont.woff2" as="font"
      type="font/woff2" crossOrigin="anonymous" />
```

`crossOrigin` اجباری است حتی برای فونت خودمیزبان، وگرنه دو بار دانلود می‌شود.

### افکت نوار نئون

```css
@property --neon-angle { syntax: "<angle>"; inherits: false; initial-value: 0deg; }
@keyframes neon-spin { to { --neon-angle: 360deg; } }

.neon-edge::before {
  content: ""; position: absolute; inset: 0; border-radius: inherit;
  padding: 1px;                        /* ضخامت نوار */
  background: conic-gradient(from var(--neon-angle), /* … */);
  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
          mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite: xor;
          mask-composite: exclude;
  opacity: 0; transition: opacity 260ms;
}

@media (hover: hover) and (pointer: fine) {
  .neon-edge:hover::before { opacity: 1; animation: neon-spin 2.6s linear infinite; }
}
.neon-edge:active::before { opacity: 1; animation: neon-spin 2.6s linear infinite; }
```

**سه چیز که بی‌صدا می‌شکنند:**
- بدون `@property` انیمیشن می‌پرد (متغیر معمولی درون‌یابی نمی‌شود)
- بدون `-webkit-` در سافاری کل کارت سبز می‌شود به‌جای قابش
- بدون گارد `(hover: hover)` روی موبایل نوار گیر می‌کند (مرورگر موبایل hover را شبیه‌سازی و نگه می‌دارد)

### چهار درس دیزاین که گران تمام شدند

**۱. `flex-row-reverse` بدون قید بریک‌پوینت.** در RTL آیکون را چپ می‌گذارد؛ در ستون باریک خوب است، در کارت تمام‌عرض آیکون به لبه پرتاب می‌شود.

**۲. `transform-style: preserve-3d` روی کارت.** تا وقتی فرزند لایه‌داری نداشت بی‌اثر بود؛ با اضافه شدن شبه‌عنصر، کارت در فضای سه‌بعدی والد کج شد.

**۳. حلقهٔ فوکوس سراسری با `border-radius` ثابت.** روی ورودی داخل یک قرص گرد، مستطیل سبز وسط فیلد می‌کشد.

**۴. `position: fixed` روی iOS زیر زوم.** نوار پایین باید با الگوی «پوستهٔ اپ» ساخته شود:
```css
@media (width < 64rem) {
  html, body { height: 100%; overflow: hidden; }
  .scroller  { flex: 1; min-height: 0; overflow-y: auto; overscroll-behavior-y: contain; }
  .tabbar    { position: relative; }
}
```

### انتخاب نشدن متن روی لمس

```css
@media (pointer: coarse) {
  body { -webkit-user-select: none; user-select: none;
         -webkit-tap-highlight-color: transparent; }
  input, textarea, [contenteditable="true"] { -webkit-user-select: text; user-select: text; }
}
```

استثنای ورودی‌ها اجباری است، وگرنه کاربر نمی‌تواند متنی را که تایپ کرده اصلاح کند.

---

## بخش هفت — استقرار

### ساختار

```
سرور (Ubuntu)
├── /opt/psyg/              کد پروژه
│   ├── .env                ⚠️ فقط روی سرور، هیچ‌وقت در ریپو
│   └── docker-compose.yml
├── والیوم داکر            catalog.json، admin.json، subscribers.json، articles.json
└── Cloudflare Tunnel       بدون باز کردن پورت
```

### `deploy.ps1` چه می‌کند

```
۰. تست کامل روی دستگاه خودت (typecheck + unit + build + e2e)
   ↓ اگر قرمز شد، هیچ‌چیزی آپلود نمی‌شود
۱. ساخت zip از src/ و tests/
۲. آپلود با scp
۳. باز کردن + حذف فایل‌هایی که در آرشیو نیستند
۴. docker compose up -d --build
۵. نوشتن وضعیت در فایل تا واچر بتواند بخواند
```

**نکتهٔ ۳ مهم است:** `unzip -o` فایل‌های حذف‌شده را پاک نمی‌کند. دو بار کامپوننت حذف‌شده روی سرور زنده مانده بود.

**نکتهٔ trap:** وضعیت پیش‌فرض `FAILED` است و فقط در آخرین خط `DONE` می‌شود. اجرای نصفه‌کاره هرگز موفق به‌نظر نمی‌رسد.

### متغیرهای محیطی

```bash
# زمان بیلد (در باندل حک می‌شوند)
NEXT_PUBLIC_SITE_URL=https://example.com
NEXT_PUBLIC_GA_ID=

# زمان اجرا (با restart عوض می‌شوند)
PSYG_REQUIRE_CATALOG=1
PSYG_DATA_DIR=/data
PSYG_INGEST_TOKEN=       # هر کدام جدا
PSYG_CONTENT_TOKEN=
PSYG_SUBSCRIBERS_TOKEN=
PSYG_MCP_TOKEN=
PSYG_ADMIN_PASSWORD=
N8N_ASSISTANT_WEBHOOK_URL=
RESEND_API_KEY=
```

**هر متغیر تازه باید در `docker-compose.yml` هم اعلام شود** وگرنه به کانتینر نمی‌رسد.

---

## بخش هشت — امنیت

| قاعده | چرا |
|---|---|
| `/api/ingest` بدنه نمی‌پذیرد | توکن لو رفته فقط یعنی به‌روزرسانی زودهنگام |
| هر توکن جدا | لو رفتن یکی، بقیه را باز نمی‌کند |
| مقایسهٔ `timingSafeEqual` | جلوگیری از حملهٔ زمانی |
| بدون توکن = غیرفعال | فراموش کردن تنظیم، نباید یعنی درِ باز |
| سقف نرخ در میدل‌ور | Nginx در مسیر Cloudflare Tunnel نیست |
| هیچ تگی در محتوای تولیدشده | مدل می‌تواند HTML بنویسد |
| ریپو خصوصی | اسکریپت‌های دیپلوی IP سرور را دارند |

**سقف نرخ به تناسب هزینه:**
```
/api/search      120   سبک و فقط‌خواندنی
/api/assistant    12   هر فراخوانی یک درخواست واقعی به مدل
/api/contact       3   هر فراخوانی یک ایمیل واقعی
/api/admin/login   5   تنها مسیری که رمز می‌سنجد
```

---

## بخش نه — کارایی

اندازه‌گیری روی سایت زنده، نه حدس:

| چیز | عدد | یعنی چه |
|---|---|---|
| `/api/health` | ۱۹۸ms | کف شبکه + تونل |
| صفحهٔ اصلی | ۲۷۰ms | فقط ۷۰ms کار واقعی سرور |
| `domComplete` | ۲۵۷۴ms | دو ثانیه در مرورگر می‌گذرد |
| شروع دانلود فونت | ۱۱۹۲ms | ← بزرگ‌ترین برد |

**نتیجه: ارتقای پلن سرور تقریباً بی‌اثر است.** فقط می‌تواند آن ۷۰ میلی‌ثانیه را کم کند.

**روش درست:** قبل از هر بهینه‌سازی، یک مسیر بی‌کار (`/api/health`) را با صفحهٔ واقعی مقایسه کن. تفاوتشان کار واقعی سرور است؛ بقیه شبکه است.

بردهای واقعی: preload فونت، `preconnect` به CDN تصاویر، `minimumCacheTTL` تصویر، `optimizePackageImports` برای کتابخانهٔ آیکون.

---

## بخش ده — چه چیزی عوض می‌شود

### حتماً عوض می‌شود
- `lib/types.ts` — مدل دامنه
- `lib/reference.ts` — دسته‌ها، فروشندگان، ویژگی‌ها
- `lib/site.ts` — برند، دامنه، متن‌ها
- `lib/affilio.ts` → آداپتور منبع تو
- `styles/globals.css` — پالت، فونت، شعاع
- `public/fonts/`، `public/icon-*.png`
- پرامپت ورک‌فلوهای محتوا

### تقریباً دست‌نخورده می‌ماند
- `catalog-store.ts`، `admin-store.ts`، `article-store.ts`، `subscriber-store.ts`
- `middleware.ts`، `mcp/`، `security-audit.ts`
- کامپوننت‌های `ui/` و `layout/`
- `docker-compose.yml`، `Dockerfile`، `deploy.ps1`
- ساختار همهٔ تست‌ها

### بستگی به دامنه دارد

اگر محصولاتت **قیمت متغیر** ندارند (مثلاً مقاله یا خدمات)، این‌ها بی‌معنا می‌شوند: `priceStanding`، نمودار، هشدار افت، خبرنامهٔ تخفیف. جایشان چیز دیگری می‌آید — ولی **همان اصل**: فقط چیزی را نشان بده که اندازه گرفته‌ای.

---

## بخش یازده — ترتیب ساخت

```
۱. مدل دامنه          types.ts، reference.ts، دادهٔ نمونه
۲. توکن‌های دیزاین    globals.css، فونت، لوگو
۳. کامپوننت‌های پایه  Card، Button، Input، Badge
۴. صفحه‌ها            خانه، فهرست، جزئیات، دربارهٔ ما
۵. لایهٔ داده         catalog-store، admin-store + تست
۶. اندپوینت‌ها        ingest، feed، search + توکن و سقف نرخ
۷. استقرار            Dockerfile، compose، deploy، تونل
۸. آداپتور منبع       اتصال به API واقعی
۹. ورک‌فلوها          sync، محتوا، خبرنامه، تلگرام
۱۰. صیقل             انیمیشن، موبایل، کارایی
```

**۵ را قبل از ۸ انجام بده.** لایهٔ داده با دادهٔ نمونه ساخته و تست شود؛ آن‌وقت وقتی منبع واقعی وصل شد، اگر چیزی خراب بود می‌دانی از کجاست.

---

## بخش دوازده — چه چیزی به چت تازه بدهی

**همین فایل**، به‌علاوه:

1. **حوزه** — چه می‌فروشی/رصد می‌کنی، و چه ادعایی می‌کنی
2. **منبع داده** — API، فید، یا ورود دستی؟ یک نمونهٔ واقعی از پاسخش
3. **لوگو** — SVG یا PNG با پس‌زمینهٔ شفاف
4. **پالت رنگ** — دست‌کم رنگ برند؛ بقیه ساخته می‌شود
5. **فونت** — فایل `woff2`
6. **دامنه**
7. **زبان و جهت** — فارسی/RTL یا چیز دیگر
8. **دسته‌بندی‌ها** — فهرست اولیه

### چیزی که باید بگویی و معمولاً یادت می‌رود

> **«اگر جایی نمی‌شود صادقانه ادعا کرد، ادعا نکن — حتی اگر صفحه خالی‌تر شود.»**

این یک جمله بیشتر از بقیهٔ این سند ارزش دارد.

### دو نمونه

**پوشاک:**
> «سایت رصد قیمت پوشاک. دسته‌ها: مردانه، زنانه، بچگانه، کفش، اکسسوری. منبع: [X]. رنگ برند `#e11d48`. فونت: [پیوست]. مثل سای‌جی، ولی به‌جای «افت قیمت» می‌خواهم «موجودی سایز» را هم رصد کند.»

**زغال:**
> «سایت مقایسهٔ عمدهٔ زغال. خریدار عمده است نه مصرف‌کننده، پس قیمت پلکانی مهم است. رنگ برند `#f59e0b`. منبع فعلاً ورود دستی از پنل ادمین است.»

---

## پیوست — بیست درسی که گران تمام شدند

1. کدی که کار می‌کند، خطا نمی‌دهد و چیز اشتباه نشان می‌دهد، بدترین نوع باگ است
2. نگهبانی که فیلدی غیر از آنچه محافظت می‌کند را می‌سنجد، نگهبان نیست
3. شکل پاسخ خطا، شکل پاسخ موفق نیست
4. تستی که به ترتیب قواعد در فایل حساس باشد، دیر یا زود دروغ می‌گوید
5. تستی که سورس می‌خواند، اول باید کامنت‌ها را حذف کند
6. راه‌حل تازه که اضافه شود و قدیمی برداشته نشود، خودش می‌شود مشکل
7. فرضی که سال‌ها بی‌ضرر بوده، با اضافه شدن چیز تازه معنا پیدا می‌کند
8. افکتی که در جاوااسکریپت زندگی می‌کند، در جستجوی CSS پیدا نمی‌شود
9. قاعده‌ای که در یک عرض درست است، در عرض دیگر غلط است
10. متغیر محیطی که به کانتینر نرسد، هیچ خطایی نمی‌دهد
11. کاربر باید بداند چیزی فرستاده نشده — «موفق» دروغ گران است
12. خالی از جعلی بهتر است
13. صفحهٔ خالی بهتر از صفحه‌ای است که چیز اشتباه نشان می‌دهد
14. چیپی که به بن‌بست می‌رسد، بدتر از نبودنش است
15. کانالی که فقط برای پر شدن پست می‌گذارد، ساکت می‌شود
16. قبل از بهینه‌سازی، اندازه بگیر — کف شبکه را از کار سرور جدا کن
17. دو نمونهٔ واقعی را مقایسه کن قبل از اینکه روی فرض داده منطق بسازی
18. عدد را کد بنویسد، نه مدل زبانی
19. مدل خروجی‌اش را در قالب‌های مختلف می‌دهد؛ همه را باز کن، و اگر نشد خام نشان نده
20. وقتی نمی‌دانی، حالت سوم بساز — نه اینکه به یکی از دو حالت موجود بچسبانی
