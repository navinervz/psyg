import type { Metadata } from "next";
import { ViewportProbe } from "@/components/debug/ViewportProbe";

/**
 * صفحه‌ی تشخیص viewport — موقتی.
 *
 * ─────────────────────────────────────────────────────────────────────
 * چرا وجود دارد
 * ─────────────────────────────────────────────────────────────────────
 * نوار پایین موبایل سه بار «درست» شد و سه بار درست نبود:
 *
 *   ۱. `env(safe-area-inset-bottom)` — روی آیفون همیشه ۳۴ پیکسل است
 *      حتی وقتی نوار مرورگر همان ناحیه را پوشانده، پس خودش شکاف ساخت.
 *   ۲. لایه‌ی هم‌رنگ زیر نوار — فرض می‌کرد نوار سر جایش است و فقط
 *      زیرش خالی است.
 *   ۳. گره زدن به `visualViewport` — منطقش درست است، ولی نمی‌دانیم
 *      سافاری روی این گوشی `position: fixed` را نسبت به کدام viewport
 *      حساب می‌کند. اگر خودش قبلاً درست حساب می‌کرده، این اصلاح
 *      شکاف را دو برابر کرده.
 *
 * هر سه حدس بودند، چون این باگ فقط روی سافاری iOS رخ می‌دهد و ابزار
 * ما به آن نمی‌رسد: پنجره‌ی کروم maximize بود و resize اثر نکرد، و
 * iframe به CSP خورد.
 *
 * این صفحه به‌جای حدس، عددها را روی خود گوشی نشان می‌دهد. یک
 * اسکرین‌شات از آن، سه فرضیه را به یک جواب تبدیل می‌کند.
 *
 * بعد از حل شدن باگ حذف می‌شود.
 */

export const metadata: Metadata = {
  title: "تشخیص viewport",
  // این صفحه نباید در نتایج جستجو بیاید
  robots: { index: false, follow: false },
};

export const dynamic = "force-dynamic";

export default function ViewportDebugPage() {
  return <ViewportProbe />;
}
